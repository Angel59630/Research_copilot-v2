# AI 研究助手需求规格说明

## 1. 文档目的

本文档定义 AI 研究助手第一版的产品范围、技术边界、功能行为、数据规则和验收标准，作为后续设计与实现的依据。

## 2. 产品定位与范围

面向单个用户在 Windows 本机运行的论文研究辅助工具。用户可以导入本地 PDF，或通过项目内置的 Arxiv MCP 服务检索和导入论文；随后对论文进行分组、检索，并围绕单篇论文或某个分组开展可持久化的多轮问答。

### 2.1 第一版目标

- 管理本地论文库、标签和一级分组。
- 支持本地 PDF 与 Arxiv 两种导入方式。
- 对可提取文本的 PDF 建立本地向量索引。
- 支持单论文问答和分组问答。
- 回答优先依据论文，并标注论文名称与 PDF 物理页码。
- 支持多会话、模型切换、流式回答和上下文压缩。
- 所有接口和内部关键调用具备可追踪、隐私优先的日志。

### 2.2 第一版不包含

- 用户注册、登录、权限和多用户数据隔离。
- 扫描版 PDF 的 OCR。
- 独立的论文摘要生成、编辑和删除功能。
- 内置 PDF 阅读器、引用点击跳转和原文高亮。
- 多级分组。
- 论文去重、合并、PDF 替换和 Arxiv 版本更新。
- 数据备份与恢复。
- Docker 部署。
- 平板和手机端完整适配。
- 自动化测试框架及自动化测试代码。

## 3. 运行环境与技术栈

### 3.1 运行方式

- Windows 本地运行，前后端分别启动。
- Ollama 作为宿主机上的独立本地服务运行。
- 第一版按单用户、单实例设计。
- 桌面浏览器建议最小窗口宽度为 1280px。
- 界面仅提供中文；论文原始元数据保持原语言。

### 3.2 后端技术栈

- FastAPI
- LangChain
- LangGraph
- Agentic RAG
- SQLite
- Chroma
- Ollama Embedding
- DeepSeek OpenAI 兼容接口
- MCP Python SDK

### 3.3 前端技术栈

- Vue 3
- TypeScript
- Vite
- Element Plus
- Vue Router
- Pinia

前端不引入 PDF.js，也不引入 Vitest、Playwright 等测试框架。

## 4. 总体架构

```text
Vue 3 桌面端界面
        │ REST API / SSE
        ▼
FastAPI 应用
├─ 论文与分组服务
├─ 会话与上下文压缩服务
├─ 后台导入任务服务
├─ LangGraph Agentic RAG
└─ Arxiv MCP Client
        │ stdio
        ▼
项目内置 Arxiv MCP Server
        │
        ▼
Arxiv 官方 API / PDF 下载
```

### 4.1 数据存储职责

- SQLite：论文元数据、标签、分组关系、导入任务、会话、消息、引用、压缩摘要和压缩位置。
- 本地文件目录：原始 PDF 和按页解析后的文本。
- Chroma：文本块向量，以及论文记录 ID、PDF 物理页码、文本块序号等检索元数据。
- `.env`：DeepSeek API Key 等敏感值。
- `config.py`：全部非敏感可配置参数及敏感参数的读取入口。

## 5. 论文导入

### 5.1 本地 PDF 上传

- 仅接受 PDF。
- 单文件默认最大 50 MB、最多 500 个 PDF 物理页，限制可通过 `config.py` 调整。
- 仅支持能够直接提取文本的 PDF。
- 检测到扫描版或无法提取有效文本时，任务失败并显示中文原因。
- 每次上传都创建新的论文记录，不进行文件哈希或内容去重。
- 新论文未加入普通分组时显示在“未分类”视图。

### 5.2 Arxiv MCP 导入

项目内置独立的 Arxiv MCP Server，FastAPI 后端通过 stdio MCP Client 调用，不要求用户另行部署 MCP 服务。

MCP Server 至少支持：

- 按关键词、标题或作者查询论文。
- 按 Arxiv ID 查询论文。
- 解析 Arxiv 论文链接。
- 获取论文元数据。
- 下载论文 PDF。

前端支持：

- 关键词、标题和作者搜索。
- 结果分页和排序。
- 显示标题、作者、摘要、发表日期和分类。
- 选择一篇或多篇论文导入。
- 输入 Arxiv ID 或链接直接导入。

每次 Arxiv 导入均创建独立论文记录，即使 Arxiv ID 或 PDF 内容相同也不去重。应用应遵守 Arxiv API 使用条款并展示必要的数据来源致谢。

### 5.3 后台处理流程

```text
创建论文记录
→ 保存 PDF
→ 校验页数
→ 按页提取文本
→ 检查有效文本
→ 提取或写入元数据
→ 按页分块
→ 调用 Ollama bge-m3 生成向量
→ 写入 Chroma
→ 标记处理完成
```

处理状态至少包括：等待、解析中、向量化中、完成、失败、已中断。

要求：

- 处理在应用内后台异步执行，上传接口不阻塞等待全部处理结束。
- 前端显示状态和失败原因。
- 失败或中断任务允许手动重试。
- 重试前清除该论文可能存在的不完整 Chroma 数据。
- 应用异常退出后，启动时将遗留的运行中任务标记为“已中断”。
- 不引入 Celery 或 Redis。

## 6. 论文元数据与文件操作

论文元数据包含：

- 标题，必填。
- 作者、发表日期、摘要、标签和备注，可选。
- 来源：本地上传或 Arxiv。
- 创建时间、更新时间和处理状态。

“摘要”字段表示论文原始摘要或用户编辑的元数据，不是独立的 AI 摘要功能。

行为要求：

- Arxiv 导入时自动填充可获得的元数据。
- 本地 PDF 导入时尝试提取元数据，用户可修正。
- 用户可编辑标题、作者、发表日期、摘要、标签和备注。
- 编辑元数据不触发重新向量化。
- 论文详情提供“下载原始 PDF”按钮。
- 不提供内置 PDF 阅读功能。
- 删除论文前必须二次确认。
- 删除论文后永久清除 PDF、分页文本、Chroma 向量、标签关系、分组关系和该论文的全部问答会话。

## 7. 分组与未分类视图

- 仅支持一级普通分组。
- 用户可新建、重命名和删除普通分组。
- 论文与普通分组为多对多关系。
- 同一篇论文可属于多个普通分组，底层 PDF 和向量数据不复制。
- “未分类”是系统生成的虚拟筛选视图，不保存为真实分组记录。
- 没有普通分组关系的论文显示在“未分类”中。
- 论文加入任意普通分组后自动从“未分类”消失。
- 删除分组后，若论文不再属于其他普通分组，则自动重新出现在“未分类”中。
- “未分类”不能重命名或删除。
- 删除普通分组前必须二次确认。
- 删除普通分组时删除其关系和该分组的全部问答会话，但不删除分组内论文，也不影响论文自己的问答会话。

## 8. 论文库搜索与筛选

支持按标题、作者和标签搜索，并按普通分组或“未分类”、处理状态和来源筛选。所有列表分页。

第一版按约 1,000 篇论文和 100 个普通分组进行设计和手工验收，该规模不是禁止继续导入的硬上限。

## 9. 问答会话

### 9.1 会话范围

- 每篇论文和每个普通分组均可创建多个独立会话。
- 会话历史持久化到 SQLite。
- 会话支持新建、重命名和删除。
- 删除会话只删除聊天历史，不影响论文、分组或向量数据。
- 分组会话每次提问均使用分组当前包含的论文。
- 分组成员变化只影响后续检索，不修改历史回答及引用。
- 分组问答只检索处理完成的论文，静默忽略其他状态的论文。

### 9.2 会话操作

- 复制回答。
- 重新生成上一条回答。
- 切换会话使用的模型。
- 手动压缩上下文。
- 中止正在生成的回答。
- 将单个会话导出为 UTF-8 Markdown。

Markdown 导出包含会话标题、论文或分组名称、模型信息、消息时间、问答正文及引用。

## 10. 模型配置与选择

### 10.1 DeepSeek 接入

- 使用 DeepSeek OpenAI 兼容接口。
- 通过 LangChain 的 OpenAI 兼容聊天模型客户端调用。
- API Key 保存于 `.env`，并提供不含真实密钥的 `.env.example`。
- API 地址、密钥环境变量名、模型名和生成参数由 `config.py` 管理。

### 10.2 动态模型列表

`config.py` 维护可选模型列表，前端通过后端安全接口动态读取，不在 Vue 中写死。

每个模型配置至少包含模型标识、界面显示名称、API Base URL、API Key 环境变量名、上下文窗口、默认生成参数和启用状态。

默认可配置 `deepseek-v4-flash` 和 `deepseek-v4-pro`，实际界面选项以 `config.py` 为准。

### 10.3 会话级模型选择

- 每个会话独立保存所选模型。
- 用户可随时切换，切换只影响后续回答。
- 回答语言默认跟随用户提问语言。
- 论文标题保留原始语言。

### 10.4 Embedding

- 使用宿主机 Ollama 和 `bge-m3`。
- Ollama 地址、模型名、超时和批处理参数由 `config.py` 管理。
- 向量索引使用本地持久化 Chroma。

## 11. Agentic RAG

采用受控 LangGraph 工作流：

```text
确定单论文或分组检索范围
→ 向量检索
→ 评估检索结果相关性
→ 证据不足时改写查询
→ 再次检索
→ 生成回答
→ 校验引用
→ 持久化消息
```

规则：

- 默认最多执行两轮“检索—评估”，次数可配置。
- 单论文问答只能检索目标论文。
- 分组问答只能检索当前分组中处理完成的论文。
- 文本分块大小、重叠、检索 `top_k`、相关性阈值和最大检索轮数由 `config.py` 管理。
- 论文证据充足时只依据论文回答。
- 论文证据不足时允许使用模型自身知识，但必须放在明确的“模型知识补充”部分，且不得伪造论文引用。

## 12. 引用规则

- PDF 按物理页进行文本提取和分块。
- 页码使用 PDF 文件中从 1 开始的物理页序号，不使用论文印刷页码。
- 每个 Chroma 文本块保存论文记录 ID、论文标题、PDF 页码和文本块 ID。
- 检索结果在单次回答中分配稳定来源编号。
- 模型使用来源编号生成回答。
- 后端校验来源编号必须来自本轮实际检索结果。
- 前端展示“论文名称，PDF 第 N 页”。
- 不提供引用跳转、PDF 阅读器或原文高亮。
- 不能通过校验的引用不得作为论文来源展示。

## 13. 流式回答

- 使用 SSE 从 FastAPI 向浏览器流式传输模型回答。
- 前端实时渲染回答内容。
- 用户可中止生成。
- 中止后的残缺模型回答不写入正式会话历史。
- SSE 事件包含请求标识、内容增量、完成状态、错误状态和最终用量信息。
- 不使用 WebSocket。

## 14. 上下文压缩

- 支持手动压缩和自动压缩。
- 自动压缩默认触发值为 250,000 token。
- 原始消息始终完整保存在 SQLite 并继续在界面展示。
- 压缩仅改变后续发送给模型的上下文。
- 会话保存累计压缩摘要和已压缩到的消息位置。
- 后续上下文由“累计压缩摘要 + 未压缩的近期消息”组成。
- 默认保留最近约 50,000 token 原始消息，具体值可配置。
- `config.py` 单独指定压缩模型，默认使用 `deepseek-v4-flash`。
- 手动和自动压缩共用同一个压缩模型。
- 压缩失败时保留原摘要和压缩位置，并提示错误。
- token 数针对实际准备发送的会话上下文，不累计重复发送产生的计费 token。
- 模型配置必须声明上下文窗口。
- 实际自动压缩阈值取“250,000 token”和“模型窗口扣除系统提示、检索上下文及输出预留量”中的较小值。

## 15. 富文本显示

问答消息支持 Markdown、代码块与语法高亮、Markdown 表格，以及 LaTeX 行内和块级公式。

数据库保存原始 Markdown。前端渲染前进行安全过滤，不执行模型输出中的不可信 HTML 或脚本。

## 16. API 设计

- 普通增删改查和文件操作使用 REST API。
- 模型回答使用 SSE。
- Arxiv MCP 只在后端内部调用，浏览器不直接访问 MCP。
- 所有列表接口支持分页。
- API 错误统一返回错误码、中文提示、`request_id` 和可选的安全详细信息。
- 前端只能读取模型标识、显示名称、上下文限制等安全配置，不能读取 API Key。
- 提供健康检查接口，分别报告 SQLite、Chroma、Ollama 和 Arxiv MCP 状态。

## 17. 日志管理

所有 REST、SSE、后台任务、LangGraph、DeepSeek、Ollama 和 MCP 操作均纳入统一日志。

### 17.1 日志内容

- 每个外部请求生成 `request_id` 并在响应中返回，内部调用继承该标识。
- 记录时间、级别、HTTP 方法、路由、状态码、耗时和客户端地址。
- 记录后台任务 ID、论文 ID、分组 ID、会话 ID、模型标识、处理阶段和耗时。
- SSE 记录连接建立、中止、完成、模型异常和最终 token 用量。
- MCP 记录工具名称、耗时和结果状态。
- 异常记录完整堆栈。
- 前端错误提示显示 `request_id`。

### 17.2 隐私与脱敏

日志不得记录用户问题、模型回答、论文正文、引用原文、PDF 内容、API Key、Authorization、Cookie 或未脱敏的本地文件绝对路径。

### 17.3 输出与轮转

- 同时输出控制台日志和 UTF-8 文件日志。
- 文件日志按大小轮转。
- 日志级别、目录、单文件大小和保留文件数由 `config.py` 管理。

## 18. 配置管理

所有可配置参数必须通过项目根目录 `config.py` 提供统一入口，包括：

- 服务主机、端口、CORS 和前端地址。
- 数据目录和存储路径。
- 文件大小、页数和类型限制。
- 后台任务并发数、超时和重试次数。
- DeepSeek 可选模型列表及默认模型。
- 压缩模型、阈值、近期消息保留量和安全预留量。
- Ollama、Embedding 和批处理参数。
- 文本分块和 Agentic RAG 检索参数。
- 外部服务超时参数。
- 日志参数。

业务代码不得散落硬编码可调参数。敏感值从 `.env` 读取，但读取、校验和向业务代码暴露的入口仍为 `config.py`。

## 19. 错误处理与一致性

- DeepSeek、Ollama、Arxiv 和 MCP 调用均设置超时。
- 外部服务异常提供明确中文错误和 `request_id`。
- 删除操作使用数据库事务维护业务数据一致性。
- 文件或向量清理失败必须记录错误，不得静默忽略。
- 论文处理重试具有幂等性，不重复写入同一论文记录的文本块。
- 后台任务失败不得影响其他论文或会话。
- 健康检查不得泄露密钥、论文内容或本地敏感路径。

## 20. 手工验收

第一版不引入自动化测试框架，不编写自动化测试代码。交付前至少完成以下手工验收：

1. 启动 FastAPI、Vue 和本地 Ollama。
2. 上传合法 PDF 并完成解析和向量化。
3. 验证超限文件、扫描件和无效 PDF 的失败提示。
4. 通过 Arxiv 搜索、Arxiv ID 和链接导入论文。
5. 编辑论文元数据、标签和备注。
6. 按标题、作者、标签、分组、状态和来源筛选。
7. 验证多分组关系和“未分类”虚拟视图。
8. 完成单论文和分组问答。
9. 验证引用只显示真实检索到的论文名称和 PDF 页码。
10. 验证论文证据不足时“模型知识补充”明确分隔。
11. 验证 SSE 流式输出和中止生成。
12. 验证多会话、模型切换、复制和重新生成。
13. 验证动态模型列表。
14. 验证手动压缩和达到阈值后的自动压缩。
15. 验证 Markdown、代码块、表格和 LaTeX 展示。
16. 验证会话导出为 UTF-8 Markdown。
17. 验证原始 PDF 下载。
18. 验证论文和分组的二次确认及级联删除。
19. 验证服务异常、日志轮转、脱敏和 `request_id` 关联。
20. 验证健康检查接口。

## 21. 第一版验收完成标准

- Windows 本地能够稳定启动前后端并连接 Ollama。
- 两种论文导入路径可用，处理状态清晰且失败可重试。
- 论文、标签、分组和“未分类”规则符合本文档。
- 单论文和分组问答能够持久化、多会话运行并流式输出。
- Agentic RAG 能在限定范围内检索、改写查询、再次检索并生成带有效页码引用的回答。
- 模型切换和上下文压缩符合本文档。
- 删除操作、隐私日志和错误提示符合本文档。
- 已执行并记录第 20 节手工验收结果。
